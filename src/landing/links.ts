import { DISCOVER_PATH } from "../lib/routes";

/** "Open Reverie" and "Ask it what to watch": Discover, which is live. */
export const APP_URL = DISCOVER_PATH;

/** "Start a Movie Jam": the jam registry, where a room is started. Movie Jam is live. */
export const JAM_URL = "/jams";
